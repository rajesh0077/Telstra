# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

platform :ios, '8.0'
pod 'SDWebImage', '~> 5.0'
target 'TelstraDemoApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TelstraDemoApp

  target 'TelstraDemoAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
